---
description: Evaluator만 단독 실행 (output/ 코드 QA 검수)
argument-hint: [추가 검수 지시 (선택)]
---

`harness/CLAUDE.md`의 "단계 3: Evaluator 호출" 섹션을 읽고 Evaluator만 단독 실행하라.
`harness/PROJECT_CONTEXT.md`를 먼저 읽어라.

1. `harness/output/` 폴더의 Swift 코드를 대상으로 검수
2. `harness/SPEC.md` 기준으로 기능 구현 여부 확인
3. `harness/agents/evaluation_criteria.md`에 따라 채점
4. `harness/QA_REPORT.md` 생성

$ARGUMENTS
